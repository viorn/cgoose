# cgoose — TUI для Goose AI Сессий

**cgoose** — это терминальный интерфейс для [Goose](https://github.com/block/goose) — вашего AI-coding агента. Вместо сырых CLI-команд он предлагает интерактивный многошаговый workflow для выбора сессий, провайдеров и моделей.

> Построен на [Clack](https://github.com/natemoo-re/clack) промптах + [picocolors](https://github.com/alexeyraspopov/picocolors), работает на [Bun](https://bun.sh).
>
> **⚠ Протестировано только на Linux.** Возможно работает на macOS (secret-tool недоступен, но file-based secrets должны работать). Windows не поддерживается.

---

## Скриншоты

| Список сессий | Новая сессия |
|:--:|:--:|
| ![sessions](sessions.jpg) | ![new session](new-session.jpg) |
| Выбор провайдера | Выбор модели |
| ![providers](providers.jpg) | ![models](models.jpg) |

---

## Зачем cgoose?

CLI Goose работает, но каждый раз при запуске сессии нужно помнить:

- Какой провайдер я использовал в прошлый раз в этом проекте?
- Какую модель?
- Как называлась сессия?

**cgoose запоминает.** Он хранит историю провайдеров и моделей для каждого проекта, показывает что вы использовали в прошлый раз, и позволяет загружать модели напрямую из API вместо того чтобы вбивать их вручную.

---

## Установка

### Вариант 1: Готовый бинарник (не требует Bun)

```bash
bash build.sh
./cgoose
```

Требует **Bun** только для сборки. Готовый бинарник запускается на любом Linux x86_64 без Bun.

### Вариант 2: Локальная установка (требуется Bun)

```bash
bash install.sh
cgoose
```

Копирует исходники + зависимости в `~/.local/lib/cgoose/` и кладёт лаунчер в `~/.local/bin/cgoose`. Убедитесь что `~/.local/bin` есть в вашем `PATH`.

### Вариант 3: Запуск из исходников (требуется Bun)

```bash
bun install
bun start
```

---

## Возможности

### Управление сессиями
- **Список сессий** для текущей директории с нечётким поиском
- **Создать новую** — своё имя или оставить пустым для авто-имени из первого сообщения
- **Продолжить** существующую сессию с полной историей
- **Удаление** — выбрать по одной или очистить все сессии для текущей директории

### Выбор провайдера
- Отсортированы по истории использования (самые частые — первые), затем по алфавиту
- Показывает последний использованный, историю и подсказки
- **Ollama** — авто-определяется если запущен локально, показывает количество моделей и поддержку инструментов
- Поддерживает любое количество провайдеров из конфига Goose
- Кастомные провайдеры (`custom_*`) показываются только если есть JSON-файл конфигурации

### Выбор модели
- **Последняя использованная модель** показана по умолчанию (`✦`)
- **История моделей** для каждого провайдера — ранее использованные под рукой
- **Загрузить из API** — запрос `GET /v1/models` к OpenAI-совместимым эндпоинтам
- **Модели Ollama** — список со значками поддержки инструментов, предупреждение если модель не поддерживает
- **Ручной ввод** с валидацией
- Найденные модели можно **сохранить в JSON-конфиг провайдера** на месте

### Секреты и авторизация
- Читает API-ключи из всех хранилищ Goose:
  - **Системная связка ключей** (libsecret — работает с GNOME Keyring, KDE Wallet, KeePassXC и др.)
  - **Файловое хранилище** (`~/.config/goose/secrets.yaml`)
  - Переменные окружения
- Если ключ отсутствует, cgoose показывает подсказку какую переменную установить

### Git Worktree
- **Новые сессии** создают выделенный **git worktree** в `<repo>/.worktree/<name>` с одноимённой веткой
- **Возобновление** находит существующий worktree и запускает Goose внутри — ручной checkout не нужен
- **Изоляция** — worktree позволяет запускать Goose в отдельном working tree, не трогая основные ветки
- **Очистка** — удаление сессии через cgoose также удаляет worktree и ветку
- Пропускается если не в git-репозитории или установлен `CGOOSE_NO_WORKTREE=1`

### Рецепты и режимы агента
- **Интеграция рецептов** — после имени сессии cgoose показывает пикер рецептов (если они установлены)
- **Последний рецепт** всегда сверху (`❶`), затем Default, затем история + алфавит
- **`initialValue`** на всех пикерах — последний рецепт, провайдер и модель пред-выбраны
- **Enter на всех шагах** = идентичная конфигурация последней запущенной сессии
- Выбранный рецепт запускается через `goose run --recipe <name> --interactive` вместо `goose session`
- История рецептов хранится для каждого проекта (сначала последние, до 10)

### Запуск
- Устанавливает `OPENAI_BASE_URL` + `OPENAI_API_KEY` для кастомных провайдеров
- Очищает конфликтующие переменные (`OPENAI_HOST`, `OPENAI_BASE_PATH`)
- Устанавливает `GOOSE_CONTEXT_LIMIT` из JSON-конфига модели провайдера
- Возобновляет сессии с `--resume --history`
- Показывает рецепт в сводке запуска, если выбран
- Запускает `goose session` (или `goose run --recipe`) с `stdio: inherit`, пробрасывает код возврата

---

## Workflow

1. **Выберите сессию** — из существующих для этой директории, создайте новую или удалите
2. **Назовите сессию** — своё имя или оставьте пустым для авто-имени из первого сообщения
3. **Выберите рецепт** (если установлены) — последний использованный рецепт сверху (`❶`), затем Default, затем история + алфавит
4. **Выберите провайдера** — отсортированы по истории, Ollama показывается если запущен
5. **Выберите модель** — последняя использованная, история, загрузка из API или ручной ввод
6. **Context limit** (если новая модель для кастомного провайдера) — введите токены или примите значение по умолчанию
7. **Запуск** — экран сводки → `goose session` (или `goose run --recipe`) стартует в том же терминале

**Enter на каждом шаге** создаёт новую сессию с теми же рецептом, провайдером и моделью, что и последняя — никакой навигации не нужно.

Нажатие **Esc** на любом шаге возвращает на шаг назад (не отменяет).

---

## Расположение данных

| Что | Где |
|------|-------|
| Конфиг провайдеров | `~/.config/goose/config.yaml` |
| Кастомные провайдеры | `~/.config/goose/custom_providers/*.json` |
| Секреты (связка) | Системная связка ключей через libsecret (сервис: `secrets@goose:default`) |
| Секреты (файл) | `~/.config/goose/secrets.yaml` |
| История проектов | `~/.config/cgoose/projects/<дир>-<хэш>.json` |
| Git worktree | `<repo>/.worktree/<name>` (для каждого проекта, в .gitignore) |
| База сессий | `~/.local/share/goose/sessions/sessions.db` |

---

## Флаги CLI

Запускайте cgoose с односимвольным режимом (как в tmux):

| Режим | Что делает |
|-------|------------|
| `a` | **Авто-возврат** — сразу к запуску с последней сессией, провайдером и моделью |
| `n` | **Новая сессия** — пропускает выбор, создаёт новую с последним провайдером и моделью |
| `s` | **Только сессии** — только выбор сессии, шаги провайдера и модели пропускаются |

При использовании режимов `a`, `n` или `s` последний использованный **рецепт** также восстанавливается вместе с провайдером и моделью.

Если для текущей директории нет истории проекта, флаг игнорируется и запускается полный workflow.

```bash
cgoose a    # продолжить последнюю сессию (с последним рецептом/провайдером/моделью)
cgoose n    # новая сессия с последним рецептом/провайдером/моделью
cgoose s    # выбрать сессию, затем сразу запуск
```

## Скрипты

| Скрипт | Что делает |
|--------|------------|
| `build.sh` | Собирает самодостаточный бинарник (`./cgoose`) со встроенным Bun |
| `install.sh` | Устанавливает исходники в `~/.local/lib/cgoose/` + лаунчер в `~/.local/bin/cgoose` |

---

## Лицензия

MIT